{
  "name": "demo-recorder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Portable Agent Skill that launches a project, drives it in a headless browser with a visible cursor and step delays, records a real WebM demo video, transcodes it to mp4, and supports a review-and-fix loop.",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "1.62.0-alpha-2026-07-08"
  }
}
